<div class="iframe-container">
  <iframe 
    src="/chart-frame.html" 
    title="Binding Curve Figure"
    loading="eager"
  ></iframe>
</div>

<style>
  .iframe-container {
    width: 100%;
    height: 400px;
    background: transparent;
    overflow: hidden;
    position: relative;
    
    /* Optional: Fade in to hide the split-second load */
    animation: fadeIn 0.5s ease-out;
  }

  iframe {
    width: 100%;
    height: 100%;
    border: none;
    /* This ensures the scrollbars don't appear momentarily */
    overflow: hidden; 
  }

  @keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
  }
</style>