---
import BaseLayout from '../layouts/BaseLayout.astro';
import BindingCurve from '../components/BindingCurve.astro';
---

<BaseLayout 
  title="Analysis"
  description="Analysis workflows for exploring data, fitting models, and quality control.">
  
  <main>
    <div class="page-header">
      <div class="header-text">
        <h1>Analysis</h1>
        <p class="subtitle">Quantitative modeling and reproducibility workflows.</p>
      </div>
      
      <a href="https://github.com/jarohe318" class="github-button mono-meta" target="_blank" rel="noopener noreferrer">
        <span>GitHub</span> <span class="arrow">→</span>
      </a>
    </div>

    <section class="figure-container">
      <div class="figure-frame">
        <BindingCurve />
      </div>
      <figcaption class="figure-caption mono-meta">
        <strong class="caption-label">Figure 2. Saturation Binding Analysis.</strong> 
        Raw experimental data (black) fitted to a 4-parameter logistic (4PL) model (metallic gold). 
        The resulting <em>K</em><sub>d</sub> of 5.01 nM demonstrates high-affinity interaction, validated by 
        low residual variance.
      </figcaption>
    </section>

    <section class="ledger-wrapper">
      
      <article class="ledger-entry">
        <div class="entry-sidebar">
          <span class="tech-primary mono-meta">Python</span>
          <span class="status-tag">Jupyter</span>
        </div>
        <div class="entry-content">
          <div class="title-row">
            <h2 class="entry-title">Binding Curve Analysis</h2>
            <a href="#" class="external-link" aria-label="View Repo">↗</a>
          </div>
          <p class="entry-body">
            Backend logic for the interactive regression model above. A reusable pipeline for 
            tabular data ingestion, outlier detection via Z-score thresholds, and 4PL 
            curve fitting with Hill slope validation.
          </p>
          <div class="tag-cloud">
            <span class="tag">scipy</span>
            <span class="tag">pandas</span>
            <span class="tag">matplotlib</span>
          </div>
        </div>
      </article>

      <article class="ledger-entry">
        <div class="entry-sidebar">
          <span class="tech-primary mono-meta">R Stats</span>
          <span class="status-tag">Automation</span>
        </div>
        <div class="entry-content">
          <div class="title-row">
            <h2 class="entry-title">Automated Reporting Pipeline</h2>
            <a href="#" class="external-link" aria-label="View Repo">↗</a>
          </div>
          <p class="entry-body">
            Developed a reporting engine using R/RDKit that generates structure-annotated 
            slide decks directly from raw plate reader data. Standardized the visualization 
            of dose-response curves and QC metrics for team review.
          </p>
          <div class="tag-cloud">
            <span class="tag">RDKit</span>
            <span class="tag">Report Generation</span>
            <span class="tag">Workflow Opt</span>
          </div>
        </div>
      </article>

      <article class="ledger-entry">
        <div class="entry-sidebar">
          <span class="tech-primary mono-meta">Pipeline</span>
          <span class="status-tag">Script</span>
        </div>
        <div class="entry-content">
          <div class="title-row">
            <h2 class="entry-title">Structural Visualization</h2>
            <a href="#" class="external-link" aria-label="View Repo">↗</a>
          </div>
          <p class="entry-body">
            Programmatic generation of publication-quality structural figures. Automates the 
            mapping of Structure-Activity Relationship (SAR) data directly onto 3D surfaces 
            to elucidate binding pocket dynamics.
          </p>
          <div class="tag-cloud">
            <span class="tag">ChimeraX</span>
            <span class="tag">RDKit</span>
            <span class="tag">PyMOL</span>
          </div>
        </div>
      </article>

    </section>
  </main>
</BaseLayout>

<style>
  main {
    max-width: 1000px;
    margin: 0 auto;
    padding: 2rem;
  }

  /* HEADER */
  .page-header {
    margin-bottom: 4rem;
    padding-bottom: 1rem;
    border-bottom: 1px solid var(--border);
    display: flex;
    justify-content: space-between;
    align-items: flex-end;
  }

  h1 {
    font-size: 3rem;
    font-weight: 700;
    color: var(--text-main); 
    margin: 0;
    letter-spacing: -0.05rem;
  }

  .subtitle {
    font-size: 1.15rem;
    color: var(--text-muted);
    margin-top: 0.5rem;
    font-weight: 400;
  }
  
  .github-button {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    text-decoration: none;
    color: var(--text-main);
    border: 1px solid var(--border);
    padding: 0.5rem 1rem;
    border-radius: 4px;
    font-size: 0.85rem;
    transition: all 0.2s ease;
    margin-bottom: 0.5rem;
  }
  
  .github-button:hover {
    border-color: var(--text-main);
    background: var(--bg-surface);
  }

  /* FIGURE HERO */
  .figure-container {
    margin-bottom: 5rem;
  }

  .figure-frame {
    border: 1px solid var(--border);
    padding: 0.5rem;
    background: var(--bg-surface);
    border-radius: 4px;
  }

  .figure-caption {
    margin-top: 1rem;
    font-size: 0.9rem;
    color: var(--text-muted);
    padding-left: 1rem;
    border-left: 2px solid var(--border);
    line-height: 1.5;
  }

  .caption-label {
    color: var(--text-main);
    font-weight: 600;
  }

  /* LEDGER */
  .ledger-wrapper {
    display: flex;
    flex-direction: column;
    gap: 4rem;
  }

  .ledger-entry {
    display: grid;
    grid-template-columns: 140px 1fr;
    gap: 2rem;
    align-items: flex-start;
  }

  /* SIDEBAR: Unified Petrol Blue */
  .entry-sidebar {
    text-align: right;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    padding-top: 0.35rem;
    border-right: 2px solid var(--accent-primary);
    padding-right: 1.5rem;
  }

  .tech-primary {
    font-size: 0.9rem;
    color: var(--text-main);
    font-weight: 500;
  }

  .status-tag {
    font-size: 0.75rem;
    font-family: var(--font-mono);
    color: var(--accent-primary);
    opacity: 0.9;
  }

  /* CONTENT */
  .title-row {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    margin-bottom: 0.75rem;
  }

  .entry-title {
    font-size: 1.5rem;
    color: var(--text-main);
    letter-spacing: -0.01em;
    margin: 0;
  }

  .external-link {
    font-family: var(--font-mono);
    text-decoration: none;
    color: var(--text-muted);
    font-size: 1.25rem;
    line-height: 1;
    transition: color 0.2s;
  }
  
  .external-link:hover {
    color: var(--accent-primary);
  }

  .entry-body {
    font-size: 1.05rem;
    color: var(--text-muted);
    line-height: 1.6;
    margin-bottom: 1.5rem;
  }

  /* TAGS */
  .tag-cloud {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
  }

  .tag {
    font-size: 0.75rem;
    font-family: var(--font-mono);
    color: var(--accent-primary);
    background: color-mix(in srgb, var(--accent-primary), transparent 92%);
    padding: 0.25rem 0.6rem;
    border-radius: 4px;
    border: 1px solid transparent;
    transition: all 0.2s ease;
  }

  .tag:hover {
    border-color: var(--accent-primary);
    background: transparent;
  }

  /* MOBILE */
  @media (max-width: 768px) {
    .page-header { flex-direction: column; align-items: flex-start; gap: 1.5rem; }
    h1 { font-size: 2.5rem; }
    .ledger-entry { grid-template-columns: 1fr; gap: 0.5rem; padding-bottom: 2rem; border-bottom: 1px solid var(--border); }
    .ledger-entry:last-child { border-bottom: none; }
    
    .entry-sidebar { 
      flex-direction: row; 
      text-align: left; 
      align-items: center; 
      gap: 0.75rem; 
      padding-top: 0; 
      border-right: none; 
      border-left: 2px solid var(--accent-primary); 
      padding-left: 0.75rem; 
      padding-right: 0;
      margin-bottom: 0.5rem; 
    }
  }
</style>