---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout 
  title="Heat Shock Pineapple"
  description="Computational Biochemist specializing in assay development and data analysis.">
  
  <main class="home-container">
    <section class="hero-wrapper">
      
      <div class="text-content">
        <div class="identity-block">
          <h1 class="name-title">Jason Hernandez</h1>
          <h2 class="role-title mono-meta">Computational Biochemist</h2>
        </div>

        <div class="value-prop">
          <p class="lead-text">
            Advancing molecular discovery by bridging <span class="highlight">wet lab 
            rigor</span> with <span class="highlight">computational scale</span>.
          </p>
          <p class="body-text">
            I design reproducible protein assays, engineer high-throughput screening workflows, 
            and build Python pipelines to make complex data actionable.
          </p>
        </div>

        <div class="cta-row">
          <a href="/science" class="btn-primary">View Research</a>
          <a href="/code" class="btn-secondary">See Code</a>
        </div>
      </div>

      <div class="visual-content">
        <div class="portrait-frame">
          <img 
            src="/images/me/portrait.jpg" 
            alt="Jason Hernandez" 
            class="home-portrait"
            loading="eager" 
          />
          <div class="orbit-ring"></div>
        </div>
        
        <a 
          href="https://www.linkedin.com/in/jrh318" 
          target="_blank" 
          rel="noopener noreferrer" 
          class="status-badge mono-meta"
        >
          <span class="status-dot"></span> Open for collaboration
        </a>

        <div class="quote-card">
          <p>“Nearly everything is really interesting if you go into it deeply enough.”</p>
          <cite>— Richard Feynman</cite>
        </div>
      </div>

    </section>
  </main>
</BaseLayout>

<style>
  /* === LAYOUT FIX === */
  .home-container {
    width: 100%;
    /* Matches Header/Footer max-width */
    max-width: 1200px; 
    margin: 0 auto;
    padding: 0 2rem;
    min-height: 80vh;
    display: flex;
    align-items: center;
  }

  .hero-wrapper {
    display: grid;
    grid-template-columns: 1.1fr 0.9fr; 
    gap: 4rem;
    align-items: center;
    width: 100%;
  }

  /* === TEXT COLUMN === */
  .text-content {
    display: flex;
    flex-direction: column;
    gap: 2.5rem;
  }

  .name-title {
    font-size: 4rem;
    line-height: 1;
    margin-bottom: 0.75rem;
    color: var(--text-main);
    letter-spacing: -0.03em;
    font-weight: 700;
  }

  .role-title {
    font-size: 1rem;
    color: var(--accent-primary);
    display: inline-block;
    padding-bottom: 0.5rem;
    border-bottom: 2px solid var(--accent-secondary);
  }

  .lead-text {
    font-family: var(--font-serif);
    font-size: 1.5rem;
    line-height: 1.35;
    color: var(--text-main);
    margin-bottom: 0.75rem;
  }

  .highlight {
    background: linear-gradient(to bottom, transparent 65%, rgba(45, 90, 69, 0.15) 65%);
    color: inherit;
    padding: 0 4px;
  }

  .body-text {
    font-size: 1.1rem;
    color: var(--text-muted);
    max-width: 48ch;
    line-height: 1.6;
  }

  /* === BUTTONS === */
  .cta-row {
    display: flex;
    gap: 1rem;
  }

  .btn-primary, .btn-secondary {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.8rem 1.6rem;
    border-radius: 4px;
    font-family: var(--font-sans);
    font-weight: 600;
    text-decoration: none;
    transition: all 0.2s ease;
  }

  .btn-primary {
    background: var(--accent-primary);
    color: #ffffff;
    border: 1px solid var(--accent-primary);
  }
  .btn-primary:hover {
    background: var(--text-main);
    border-color: var(--text-main);
    transform: translateY(-1px);
  }

  .btn-secondary {
    background: transparent;
    color: var(--text-main);
    border: 1px solid var(--border);
  }
  .btn-secondary:hover {
    border-color: var(--accent-primary);
    color: var(--accent-primary);
  }

  /* === VISUAL COLUMN === */
  .visual-content {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    position: relative;
  }

  .portrait-frame {
    position: relative;
    width: 280px;
    height: 280px;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 1.5rem;
  }

  .home-portrait {
    width: 240px;
    height: 240px;
    border-radius: 50%;
    object-fit: cover;
    z-index: 2;
    border: 1px solid rgba(0,0,0,0.1); 
  }

  .orbit-ring {
    position: absolute;
    width: 100%;
    height: 100%;
    border: 1px dashed var(--accent-primary);
    opacity: 0.4; 
    border-radius: 50%;
    z-index: 1;
    animation: spin 60s linear infinite;
  }

  .status-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.75rem;
    color: var(--text-muted);
    margin-bottom: 2rem;
    text-decoration: none;
    padding: 0.4rem 0.8rem;
    border-radius: 20px;
    border: 1px solid var(--border);
    transition: all 0.2s ease;
  }

  .status-badge:hover {
    background: var(--bg-surface);
    border-color: var(--accent-primary);
    color: var(--text-main);
  }

  .status-dot {
    width: 8px;
    height: 8px;
    background-color: var(--accent-secondary);
    border-radius: 50%;
    box-shadow: 0 0 0 2px rgba(255, 215, 0, 0.2);
  }

  /* QUOTE CARD */
  .quote-card {
    margin-top: 1rem;
    max-width: 320px;
  }
  
  .quote-card p {
    font-family: var(--font-serif);
    font-style: italic;
    font-size: 1.1rem;
    color: var(--text-main);
    margin-bottom: 0.5rem;
  }

  .quote-card cite {
    font-family: var(--font-mono);
    font-size: 0.8rem;
    color: var(--text-muted);
    font-style: normal;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  @keyframes spin { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }

  /* === MOBILE === */
  @media (max-width: 900px) {
    .home-container {
      display: block; 
      min-height: auto;
      padding-top: 3rem;
      padding-bottom: 4rem;
    }

    .hero-wrapper {
      grid-template-columns: 1fr;
      text-align: left;
      gap: 3rem;
    }

    .text-content {
      order: 1;
      gap: 2rem;
    }

    .visual-content {
      order: 2;
      border-top: 1px solid var(--border);
      padding-top: 3rem;
      width: 100%;
    }

    .name-title { font-size: 2.8rem; }
    
    .portrait-frame { width: 220px; height: 220px; }
    .home-portrait { width: 180px; height: 180px; }
    
    .cta-row { width: 100%; flex-direction: column; }
    .btn-primary, .btn-secondary { width: 100%; }
  }
</style>